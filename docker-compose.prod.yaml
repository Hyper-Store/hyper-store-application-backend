version: '3.9'

services:
      
  app:
    build: .
    ports:
      - "5000:5000"
      - "1000:1000"
    volumes:
      - .:/home/node/app
  extra_hosts:
      - "host.docker.internal:172.17.0.1"

