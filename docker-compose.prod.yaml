version: '3.9'

services:
      
  app:
    build: .
    command: node dist/main.js
    ports:
      - "5000:5000"
      - "1000:1000"
    volumes:
      - .:/usr/src/app
    extra_hosts:
      - "host.docker.internal:172.17.0.1"

